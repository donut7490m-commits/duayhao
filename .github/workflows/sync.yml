name: Auto Sync Lottery Every Draw Date

on:
  workflow_dispatch: # ปุ่มกดรันเอง
  schedule:
    - cron: '0 10 1,16 * *' # รันอัตโนมัติทุกวันที่ 1 และ 16

jobs:
  auto-fetch:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests

      - name: Run Scraper Bot
        run: python sync_lottery.py

      - name: Commit and Push if New Data Found
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action Bot"
          git add data.json
          git diff --quiet && git diff --staged --quiet || (git commit -m "Auto-update lottery data" && git push)
